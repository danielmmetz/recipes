# OIDC authentication (optional — if unset, app runs fully anonymous with no auth)
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_ISSUER_URL=https://idp.example.com

# Base URL for redirect URI construction and cookie Secure flag
BASE_URL=http://localhost:8080
